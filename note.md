## 1026
本题使用printf进行输出。
如何使用cout进行格式化输出？

## 1031
得分14，未完全通过。

## 1033
输入两个字符串a和b时，不能使用
```
cin >> a >> b
```
这样当第一个字符为空时，第二行输入被赋值给b。
因此要采用```getline```函数。
```
getline(cin,a);
getline(cin,b);
```
## 1028
最后一步要判断是否
```
count > 0
```
若```count < 0```，不需要输出姓名。